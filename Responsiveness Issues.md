Responsiveness Issues:

- Covid-19 Page Hero Section
- Product Detail:
  - Category should be a collapsed dropdown
  - Product description should be collapsed with a read more option
  - Related services slider should loop around to the beginning when exhausted
  - On category selection, related services section still has slider, it should be the same component as in product detail related services section, use the same component, don't make a new one
- The NavBar hamburger dropdown in mobile view doesn't have the option for forms
- The bottom section in the business pages has a white gradient and white text, which kills visibility, give it the same dark gradient as the testing services pages
- On the form pages which have Agreements section (declaration and terms of service) and Digital Signature section, responsiveness is breaking and they are extending out from the right

Responsiveness Issues (Round 2):

- Covid-19 Page Hero Section has empty space under the hero image in mobile view
- Product Detail:
  - Category dropdown should collapse when category selected
  - Category description should also be collapsed with a read more option
- The bottom section in the business pages still has visibility issues, the testing services has a different gradient, we need to use that
- In the form pages when there is a validation error in the fields preventing submission, an error message should be shown above the submit button

Responsiveness Issues (Round 3):

- Covid-19 Page Hero Section has empty space under the hero image in mobile view
- In the form pages when there is a validation error in the fields preventing submission, an error message should be shown above the submit button
